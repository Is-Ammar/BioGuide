The previous publications.json mock dataset was removed to ensure all publications come from the backend API.
If you need mock data for offline development, create a new file (not committed) or reintroduce a fixture under a different name.
